<h2 class="page-header"><%= author.name %></h2>

<div class="btn-row">
    <a href="/authors/<%= author.id%>/edit" class="btn btn-primary">Edit</a>
    <%- include('../partials/_delete', { url: `/authors/${author.id}`}) %>
</div>
<% if (booksByAuthor.length > 0) { %>
    <h2 class="page-header"2>Books</h2>
    <%- include('../partials/_bookGrid.ejs', { books: booksByAuthor }) %>
<% } %>