<h2 class="page-header"><%= book.title %></h2>
<img src="<%= book.coverImagePath %>" height="150" width="100" alt="book.title" />
<div>
    Author: <%= book.author.name %>
</div>
<div>
    Publish date: <%= book.publishDate.toDateString() %>
</div>
<div>
    Page count: <%= book.pageCount %>
</div>
<div>
    Description: <%= book.description %>
</div>
<div class="btn-row">
    <a href="/books/<%= book.id%>/edit" class="btn btn-primary">Edit</a>
    <%- include('../partials/_delete', { url: `/books/${book.id}`}) %>
    <a href="/authors/<%= book.author.id%>" class="btn btn-primary">View author</a>
</div>